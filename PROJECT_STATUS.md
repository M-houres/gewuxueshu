# WuhongAI 项目状态

最后更新：2026-04-01 11:35

## 当前阶段

Phase 1 — MVP 基础

## 整体进度

13 / 18

## 今日状态（2026-04-01）

- 今日完成：
  - 后台用户详情页运营化增强：新增快捷积分调整、最近任务结果摘要、失败原因/风险段落/结果预览与跳转任务详情
  - 管理端任务详情支持 `task_id` URL 直达，便于从用户页串联到任务页深查
  - 后端全量回归测试通过：29/29 通过
  - 前端生产构建验证再次通过（含后台用户详情页重构）
  - 新增上线运维手册 `docs/GO_LIVE_RUNBOOK.md`，补齐配置中心填写、支付模式、短信/微信联调与回滚步骤
  - 推广福利页增强：新增个人推广数据概览卡、邀请/奖励状态中文化，提升普通用户可读性
  - 后台推广管理页增强：补充运营解释文案，奖励记录支持更直观的类型/状态展示
  - 修复本地/测试环境登录可用性：开发环境默认支持 debug 验证码，非生产环境开放微信 mock 二维码链路
  - 任务提交规则收口：按任务类型限制主文件格式，并新增查重/AIGC 报告全文校验
  - 前台三大任务页升级：重新拆分平台选择/主文件/报告/费用确认区，并补齐每页“近期任务”卡片
  - 推广管理页运营化改造：返佣比例改为百分比口径展示，补充规则预览文案
  - 配置中心运营化改造：重做后台配置页信息层级，补齐“上线前必看 / 运营理解 / 最近配置变更”三栏
  - 配置审计接口增强：返回管理员用户名、配置分类标签、变更字段摘要，便于运营追踪
  - 修复配置中心静态路由被动态分类路由吞掉的问题：`/admin/configs/readiness`、`/admin/configs/audit-logs` 可正常访问
  - 后端全量回归测试通过：26/26 通过（新增配置审计与静态路由顺序保护用例）
  - 任务处理链增强：查重/改写任务开始真正读取辅助报告，结果摘要补充报告指标、复核建议、改动幅度与结果预览
  - AIGC 检测结果增强：输出文本升级为结构化检测报告，结果 JSON 新增风险等级、风险段落与文本统计
  - 任务记录页升级：新增“查看结果”弹层，支持直接查看检测摘要、报告指标、风险段落和处理预览
  - 后端全量回归测试继续通过：28/28 通过（新增处理引擎结果结构用例）
  - 游客模式边界收口：任务页“查看任务记录”不再提前强制登录，先进入游客态页面；购买页补充“浏览套餐可游客、创建订单再登录”提示
  - PRD 结构化解析与任务拆分
  - FastAPI 主入口、路由、统一响应与异常处理
  - 登录、积分、任务、推广、后台接口落地
  - Celery 任务处理与失败退积分逻辑
  - Vue 前后台页面与接口联调
  - 前端构建验证通过
  - 支付回调验签与幂等处理（新增 `/api/v1/billing/callback`）
  - 算法包槽位化管理（platform/function_type 校验、`main.py` 强校验、smoke test）
  - 算法包前端管理页对齐（上传、槽位版本视图、激活操作）
  - 登录态 401 回跳优化（登录后回原页面）
  - README 新增接口文档与 curl 示例（支付回调、算法包管理）
  - 修复 SQLite 回退模式主键自增兼容（避免本地回退时新增记录失败）
  - 新增后端测试用例（支付回调 + 算法包）
  - 新增注册页 `/register`，邀请码链路改为注册页（`/register?ref=邀请码`）
  - 新增用户积分流水接口 `/api/v1/users/me/credit-transactions` 与前端页面 `/app/credits`
  - 仪表盘升级为 ECharts 图表（趋势/占比/漏斗）并接入降级横幅+手动恢复
  - 新增系统日志页 `/admin/logs`（模式切换日志 + 大模型异常日志 + CSV导出）
  - 新增后台切换与日志接口（`/admin/switch/current`、`/admin/switch/mode`、`/admin/switch/logs`、`/admin/llm-error-logs`）
  - 落库 LLM 异常日志（`llm_error_logs`）并新增 Alembic 迁移
  - 新增 super_admin 权限收敛（配置中心、算法包管理、手动模式切换）
  - 新增用户购买积分页 `/app/buy` 与个人中心页 `/app/profile`（昵称修改 + 积分流水）
  - 新增后台用户详情页 `/admin/users/:id`（基础信息、统计、积分流水、近期任务）
  - 历史记录页升级为筛选+分页+URL页码同步，后端 `/tasks/my` 支持多维筛选参数
  - 任务管理页升级为多维筛选+详情展示，后端新增 `/admin/tasks/{id}/detail`
  - 订单管理页升级为筛选+详情+退款，后端新增订单详情与退款接口
  - 新增微信扫码登录链路（`/auth/wx/qrcode`、`/auth/wx/poll/{key}`、`/auth/wx/mock-authorize`）并接入前端登录页 Tab
  - 新增支付二维码下单与轮询流程（`/billing/create-order`、`/billing/order-status/{order_no}`、`/billing/order-pay/{order_no}`）并升级购买积分弹窗
  - 历史记录页补齐失败原因查看与删除记录能力（新增 `DELETE /api/v1/tasks/{id}`）
  - 路由改为按页面懒加载，前端构建产物从单包拆分为多 chunk，首屏体积明显下降
  - 登录链路补齐设备指纹透传（`LoginReq.device_fingerprint` + 前端指纹本地持久化）并支持 `x-forwarded-for` 获取真实IP
  - 新增防刷回归测试：封禁账号登录风控日志、同IP超阈值日志、同设备超阈值日志
  - 新增格式保留自动化验证：复杂 DOCX（表格/图片/页眉页脚/多字体）处理后 XML 结构比对，仅允许文本节点变化
  - 新增任务失败退积分回归测试：验证 `task_refund` 流水、余额回滚与重复失败不重复退款
  - 本地测试环境补齐：新增 `backend/.venv_sys` 并安装 `pytest`，新增用例已通过（5 passed）
  - 新增认证接口 IP 限流：`/auth/send-code`（1小时窗口）与 `/auth/login`（10分钟窗口）
  - DOCX 格式保留测试增强：明确校验“仅正文段落改写，表格/页眉/页脚文本不变”
  - 全量后端测试补跑并修复历史用例波动（Celery mock、回调验签长度、算法包zip构造）
  - 后台仪表盘图表模块继续瘦身：移除 Pie/Funnel 依赖，改为 Line/Bar 组合图
  - 前端构建拆分优化：Vite `manualChunks` 拆分 `vendor-echarts`/`vendor-zrender`，消除 >500KB 警告
  - 生产启动安全校验：`APP_ENV=prod` 时强制检查 `JWT_SECRET` / `PAYMENT_SIGN_SECRET` / `ADMIN_INIT_PASSWORD` 非默认值
  - 计费规则配置生效：任务提交改为读取 `SystemConfig(system/billing)`，不再仅依赖硬编码费率
  - 登录配置生效增强：`send-code` 读取配置中心 `login` 分类（短信网关URL/调试开关），生产环境短信网关失败时拒绝发送
  - 支付配置生效增强：支付回调验签优先读取配置中心 `payment.callback_secret`
  - 邀请码接口补齐二维码字段：`/users/me/invite-code` 直接返回 `qrcode_data_url`
  - DOCX 文本处理增强：支持表格单元格文本处理；AIGC 文本提取包含表格内容
  - 订单状态竞态修复：`order-status` 改为只读超时视图，避免与 `order-pay` 并发覆盖
  - 登录页能力开关：微信登录默认关闭（`VITE_ENABLE_WECHAT_LOGIN=false`），避免生产误暴露 mock 流程
  - 支付页面内测标识：新增测试支付模式提示（`VITE_PAYMENT_TEST_MODE` + 后端 `payment_test_mode`）
  - 新增后端回归用例 4 个（计费配置、邀请码二维码、支付配置验签、DOCX表格提取），后端测试通过 22/22
- 今日进行中：
  - 对照 `WuhongAI_PRD_v2.0.docx` + `WuhongAI_Pages_v1.0.docx` 补齐缺口
  - 前后台路由与交互细节对齐（页面说明 v1.0）
- 今日阻塞：
  - Docker Desktop Service 在当前环境无权限启动（`com.docker.service`），容器化联调仍待系统权限侧配合

## 功能完成情况总表

| 功能模块 | PRD章节 | 状态 | 完成时间 | QA审计 | 备注 |
|---------|--------|------|---------|-------|------|
| 项目骨架与基础设施 | 2 / 11.1 | DONE | 2026-03-30 | #002 | 前后端骨架与目录完善 |
| 手机号验证码登录 | 3.2 | DONE | 2026-03-30 | #002 | 60秒冷却、错误锁定 |
| AIGC检测功能页 | 3.3 | DONE | 2026-03-30 | #002 | 已对接后端任务提交 |
| 降重复率功能页 | 3.4 | DONE | 2026-03-30 | #002 | 支持可选报告上传 |
| 改写文章功能页 | 3.5 | DONE | 2026-03-30 | #002 | 与降重流程对称 |
| 推广福利（邀请码+注册奖励） | 3.6 | PARTIAL | 2026-03-31 | #002 | 注册页链路/二维码/奖励流水已落地，仍需补齐页面细节 |
| 智能切换引擎 | 4 | PARTIAL | 2026-03-31 | #002 | LLM接入、降级切换、异常日志已落地，仍需细化监控字段 |
| 算法包管理基础 | 5 | DONE | 2026-03-31 | #004 | 已支持槽位校验、smoke、激活、前后台联动 |
| 积分系统基础 | 6 | DONE | 2026-03-30 | #002 | 扣费、充值、流水、退还 |
| 后台管理基础 | 7 | PARTIAL | 2026-04-01 | #002 | 图表、配置中心、系统日志已补齐主要闭环；仍有页面细节待对齐 |
| API规范落地 | 9 | DONE | 2026-03-30 | #002 | 统一 `{code,data,message}` |
| 非功能与安全基线 | 10 | PARTIAL | 2026-03-30 | #002 | 文件校验、鉴权完成，更多安全项待补 |

## 状态说明

TODO | IN_PROGRESS | QA_REVIEW | DONE | BLOCKED | PARTIAL

## 已知问题与风险

| ID | 问题描述 | 影响功能 | 严重程度 | 负责Agent | 计划解决日期 |
|----|----------|----------|----------|-----------|--------------|
| R-001 | Docker Engine API 500 | MySQL/Redis 容器联调 | 中 | Dev-Agent | 2026-03-31 |

## 下一步行动（Next 3 Tasks）

1. 完成 Docker 服务权限问题排查并跑通 `docker compose up -d` 全链路
2. 继续对齐审查报告剩余“真实微信/真实支付”上线方案（接入或显式分阶段说明）
3. 增补上线运维手册（密钥校验、支付模式、短信网关、回滚步骤）
